import { ClearnumberDirective } from './clearnumber.directive';

describe('ClearnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearnumberDirective();
    expect(directive).toBeTruthy();
  });
});
